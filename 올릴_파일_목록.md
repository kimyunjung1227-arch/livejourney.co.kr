# 📦 GitHub에 올릴 필수 파일 목록

GitHub Pages 배포를 위해 필요한 파일들입니다.

## ✅ 필수 파일 및 폴더

### 1. 웹 앱 소스 코드 (web 폴더)
```
web/
├── public/              # ✅ 필수 - 정적 파일
│   ├── 404.html         # ✅ 필수 - 404 에러 방지
│   ├── _redirects       # ✅ 필수 - 리다이렉트 설정
│   ├── favicon.svg      # ✅ 필수
│   ├── logo.svg         # ✅ 필수
│   ├── manifest.json    # ✅ 필수
│   └── livejourney-logo.png
├── src/                 # ✅ 필수 - 소스 코드
│   ├── api/
│   ├── components/
│   ├── contexts/
│   ├── pages/
│   ├── utils/
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
├── index.html           # ✅ 필수
├── package.json         # ✅ 필수
├── package-lock.json    # ✅ 필수
├── vite.config.js      # ✅ 필수
├── tailwind.config.js  # ✅ 필수
├── postcss.config.js   # ✅ 필수
└── README.md           # ✅ 선택
```

### 2. GitHub Actions 워크플로
```
.github/
└── workflows/
    └── deploy.yml      # ✅ 필수 - 자동 배포 설정
```

### 3. 루트 파일들
```
├── README.md                    # ✅ 선택 - 프로젝트 설명
├── GITHUB_DEPLOY_GUIDE.md       # ✅ 선택 - 배포 가이드
├── 카페_공유_가이드.md          # ✅ 선택 - 공유 가이드
├── .gitignore                   # ✅ 필수 - Git 무시 파일
└── package.json                 # ✅ 선택 (루트)
```

## ❌ 올리지 말아야 할 파일들

다음 파일들은 `.gitignore`에 의해 자동으로 제외됩니다:

- `node_modules/` - 의존성 패키지 (npm install로 설치)
- `dist/` - 빌드 결과물 (GitHub Actions가 자동 생성)
- `.env` - 환경 변수 (보안상 올리지 않음)
- `*.log` - 로그 파일
- `web/android/`, `web/ios/` - 네이티브 빌드 파일
- `backend/uploads/` - 업로드된 파일

## 🚀 올리는 방법

### 방법 1: 전체 프로젝트 올리기 (권장)
```bash
# 모든 파일 추가 (gitignore 제외)
git add .

# 커밋
git commit -m "Initial commit for GitHub Pages"

# 푸시
git push origin master
```

### 방법 2: 웹 앱만 올리기
```bash
# 웹 폴더와 GitHub Actions만 추가
git add web/ .github/ README.md GITHUB_DEPLOY_GUIDE.md 카페_공유_가이드.md .gitignore

# 커밋
git commit -m "Add web app for GitHub Pages"

# 푸시
git push origin master
```

## 📋 체크리스트

배포 전 확인사항:

- [ ] `web/package.json` 파일 존재
- [ ] `web/vite.config.js` 파일 존재 (base 경로: `/app/`)
- [ ] `web/public/404.html` 파일 존재
- [ ] `.github/workflows/deploy.yml` 파일 존재
- [ ] `.gitignore` 파일 존재
- [ ] `web/src/` 폴더에 모든 소스 코드 존재

## 💡 중요 사항

1. **node_modules는 올리지 않아도 됩니다**
   - GitHub Actions가 자동으로 `npm ci`로 설치합니다

2. **dist 폴더는 올리지 않아도 됩니다**
   - GitHub Actions가 자동으로 빌드합니다

3. **환경 변수는 GitHub Secrets에 설정**
   - Settings → Secrets and variables → Actions
   - 필요시 `VITE_API_URL`, `VITE_KAKAO_MAP_API_KEY` 등 추가

4. **최소 필수 파일만 올려도 됩니다**
   - 웹 앱 소스 코드 (`web/` 폴더)
   - GitHub Actions 워크플로 (`.github/workflows/deploy.yml`)
   - `.gitignore` 파일

## 🎯 빠른 시작

가장 간단하게 올리려면:

```bash
# 1. 현재 상태 확인
git status

# 2. 모든 변경사항 추가
git add .

# 3. 커밋
git commit -m "Deploy web app to GitHub Pages"

# 4. 푸시
git push origin master
```

그러면 GitHub Actions가 자동으로:
1. 의존성 설치 (`npm ci`)
2. 빌드 (`npm run build`)
3. GitHub Pages에 배포

## 📝 참고

- 저장소 이름이 `app`이므로 base 경로는 `/app/`입니다
- 배포 완료 후 URL: `https://kimyunjung1227-arch.github.io/app/`
