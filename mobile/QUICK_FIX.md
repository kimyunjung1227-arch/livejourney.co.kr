# 🚀 빠른 해결 방법

## 현재 문제
"Failed to download remote update" 오류

## ✅ 즉시 해결 (3가지 방법)

### 방법 1: 터널 모드 사용 (가장 확실) ⭐

**터미널에서:**
```bash
cd C:\Users\wnd12\Desktop\mvp1\mobile
npm start -- --tunnel
```

터널 모드는 인터넷을 통해 연결하므로 **같은 Wi-Fi가 아니어도 됩니다!**

### 방법 2: Expo Go 앱에서 수동 연결

1. **Expo Go 앱 실행**
2. **"Enter URL manually"** 선택
3. 다음 URL 입력:
   ```
   exp://192.168.25.27:8081
   ```

**주의:** 스마트폰과 컴퓨터가 **같은 Wi-Fi**에 연결되어 있어야 합니다!

### 방법 3: Android 에뮬레이터 사용 (네트워크 문제 없음) ⭐⭐

**새 터미널에서:**
```bash
cd C:\Users\wnd12\Desktop\mvp1\mobile
npm run android
```

이 방법은 네트워크 설정이 필요 없고 가장 안정적입니다!

---

## 🔧 추가 해결 방법

### 캐시 클리어 후 재시작
```bash
cd C:\Users\wnd12\Desktop\mvp1\mobile
npm start -- --clear
```

### LAN 모드로 시작
```bash
npm start -- --lan
```

---

## 💡 권장 순서

1. **먼저 시도:** Android 에뮬레이터 (`npm run android`)
2. **두 번째:** 터널 모드 (`npm start -- --tunnel`)
3. **세 번째:** 수동 URL 입력


