# 🔧 Expo "Failed to download remote update" 오류 해결

## 문제
Expo Go 앱에서 "Failed to download remote update" 오류가 발생합니다.

---

## ✅ 해결 방법

### 방법 1: 개발 서버 재시작 (캐시 클리어)

**터미널에서:**
```bash
cd C:\Users\wnd12\Desktop\mvp1\mobile
npm start -- --clear
```

또는:
```bash
npm start -- --reset-cache
```

### 방법 2: 네트워크 연결 확인

**스마트폰과 컴퓨터가 같은 Wi-Fi에 연결되어 있어야 합니다!**

1. **컴퓨터의 IP 주소 확인:**
   ```bash
   ipconfig
   ```
   - IPv4 주소 확인 (예: 192.168.0.100)

2. **Expo Go 앱에서 수동 연결:**
   - Expo Go 앱 실행
   - "Enter URL manually" 선택
   - 다음 형식으로 입력:
     ```
     exp://192.168.0.100:8081
     ```
     (IP 주소를 본인의 컴퓨터 IP로 변경)

### 방법 3: 터널 모드 사용

**터미널에서:**
```bash
cd C:\Users\wnd12\Desktop\mvp1\mobile
npm start -- --tunnel
```

터널 모드는 인터넷을 통해 연결하므로 같은 Wi-Fi가 아니어도 됩니다.

### 방법 4: LAN 모드로 시작

**터미널에서:**
```bash
cd C:\Users\wnd12\Desktop\mvp1\mobile
npm start -- --lan
```

### 방법 5: 포트 확인 및 변경

**기본 포트(8081)가 사용 중일 때:**
```bash
npm start -- --port 8082
```

---

## 🔍 단계별 해결

### 1단계: Metro Bundler 확인

터미널에 다음과 같은 메시지가 보여야 합니다:
```
Metro waiting on exp://192.168.x.x:8081
```

### 2단계: 방화벽 확인

Windows 방화벽이 8081 포트를 차단하고 있을 수 있습니다.

**해결:**
1. Windows 설정 → 방화벽 및 네트워크 보호
2. 고급 설정
3. 인바운드 규칙 → 새 규칙
4. 포트 선택 → TCP → 특정 로컬 포트: 8081
5. 연결 허용 선택

### 3단계: Expo Go 앱 재시작

1. Expo Go 앱 완전 종료
2. 앱 다시 실행
3. QR 코드 다시 스캔

### 4단계: 개발 서버 URL 수동 입력

Expo Go 앱에서:
1. "Enter URL manually" 선택
2. 터미널에 표시된 URL 입력
   - 예: `exp://192.168.0.100:8081`

---

## 🚀 빠른 해결 (권장 순서)

### 1. 개발 서버 재시작
```bash
cd C:\Users\wnd12\Desktop\mvp1\mobile
npm start -- --clear
```

### 2. 터널 모드로 시작
```bash
npm start -- --tunnel
```

### 3. Expo Go 앱에서 QR 코드 다시 스캔

---

## 💡 대안: Android 에뮬레이터 사용

Expo Go 앱 대신 Android 에뮬레이터를 사용하면 네트워크 문제를 피할 수 있습니다:

```bash
cd C:\Users\wnd12\Desktop\mvp1\mobile
npm run android
```

이 방법은:
- 같은 Wi-Fi 필요 없음
- 네트워크 설정 불필요
- 더 안정적

---

## 📝 체크리스트

- [ ] Metro Bundler가 실행 중인지 확인
- [ ] 스마트폰과 컴퓨터가 같은 Wi-Fi에 연결되어 있는지 확인
- [ ] 방화벽 설정 확인
- [ ] Expo Go 앱 재시작
- [ ] 개발 서버 캐시 클리어 후 재시작
- [ ] 터널 모드로 시도

---

## 🔗 참고

- 터미널에 표시된 URL을 정확히 확인하세요
- IP 주소가 변경되었을 수 있습니다 (재연결 시)
- 회사/학교 Wi-Fi는 방화벽으로 차단될 수 있습니다


