# 🚀 GitHub Pages 배포 가이드 (간단 버전)

이 가이드를 따라하면 **5분 안에** 배포할 수 있습니다!

## 📋 배포 전 확인사항

### 필수 파일 확인
다음 파일들이 있는지 확인하세요:

- ✅ `web/package.json` - 웹 앱 설정
- ✅ `web/vite.config.js` - 빌드 설정 (base: `/app/`)
- ✅ `web/public/404.html` - 404 에러 방지
- ✅ `.github/workflows/deploy.yml` - 자동 배포 설정
- ✅ `.gitignore` - Git 무시 파일

## 🎯 배포 방법 (3가지)

### 방법 1: 배치 파일 사용 (가장 쉬움) ⭐

1. **`배포하기.bat`** 파일을 더블클릭
2. Y를 입력하여 자동 커밋 및 푸시
3. 완료!

### 방법 2: Git 명령어 사용

```bash
# 1. 필수 파일만 추가
git add web/
git add .github/
git add .gitignore
git add README.md GITHUB_DEPLOY_GUIDE.md 카페_공유_가이드.md

# 2. 커밋
git commit -m "Deploy to GitHub Pages"

# 3. 푸시
git push origin master
```

### 방법 3: 전체 올리기 (간단)

```bash
# 모든 파일 추가 (.gitignore가 자동으로 필터링)
git add .

# 커밋
git commit -m "Deploy to GitHub Pages"

# 푸시
git push origin master
```

## ⚙️ GitHub 설정 (최초 1회만)

### 1단계: GitHub Pages 활성화

1. GitHub 저장소로 이동: https://github.com/kimyunjung1227-arch/app
2. **Settings** → **Pages** 클릭
3. **Source**에서 **"GitHub Actions"** 선택
4. **Save** 클릭

### 2단계: Actions 권한 설정

1. **Settings** → **Actions** → **General** 이동
2. **"Workflow permissions"**에서 **"Read and write permissions"** 선택
3. **Save** 클릭

## ✅ 배포 확인

1. **Actions** 탭에서 배포 진행 상황 확인 (5-10분 소요)
2. 배포 완료 후 접속:
   ```
   https://kimyunjung1227-arch.github.io/app/
   ```

## 📦 올릴 파일 요약

### 필수 파일
```
web/                    # 웹 앱 소스 코드 전체
.github/workflows/      # GitHub Actions 설정
.gitignore              # Git 무시 파일
```

### 선택 파일
```
README.md               # 프로젝트 설명
GITHUB_DEPLOY_GUIDE.md  # 배포 가이드
카페_공유_가이드.md     # 공유 가이드
```

### 자동 제외 (올리지 않아도 됨)
```
node_modules/          # npm install로 자동 설치
dist/                  # GitHub Actions가 자동 빌드
.env                   # 환경 변수 (보안)
```

## 🔄 업데이트 배포

코드를 수정한 후 다시 배포:

```bash
git add .
git commit -m "업데이트 내용"
git push origin master
```

자동으로 배포가 시작됩니다!

## ❓ 문제 해결

### 배포가 안 되는 경우
- **Actions** 탭에서 에러 메시지 확인
- **Settings** → **Pages**에서 Source 확인
- **Settings** → **Actions** → **General**에서 권한 확인

### 404 에러가 나는 경우
- `web/vite.config.js`의 base 경로 확인 (`/app/`)
- `web/public/404.html` 파일 존재 확인
- 브라우저 캐시 삭제 후 다시 시도

## 💡 팁

- **첫 배포는 5-10분** 걸릴 수 있습니다
- 배포 중에는 **Actions** 탭에서 진행 상황을 확인하세요
- 배포 완료 후에도 **몇 분 더 기다려야** URL이 활성화됩니다

---

**더 자세한 내용**: [GITHUB_DEPLOY_GUIDE.md](./GITHUB_DEPLOY_GUIDE.md)
